{
  "name": "blogapp",
  "version": "1.0.0",
  "description": "",
  "main": "./backend/index.js",
  "scripts": {
    "install": "cd ./backend && npm install && cd ../frontend && npm install --include=dev",
    "build": "cd ./frontend && npm run build",
    "lint": "cd ./frontend && npm run eslint",
    "test-be": "cd ./backend && npm run test",
    "start-test": "cd ./backend && npm run test:cypress",
    "test-e2e": "cypress run --project ./frontend",
    "start": "cd ./backend && npm run start"
  },
  "author": "Artem Tsarev",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^9.5.4"
  }
}
